{% extends 'base.html.twig' %}

{% block title 'Toutes les recettes' %}

{% block body %}
    <p>
        Lorem
    </p>
    <ul>
        {% for recipe in recipes%}
        <li>
            <a href="{{ path('recipe.show', {id: recipe.id, slug: recipe.slug}) }}"> Pâtes bolognaise</a>
        </li>
        {% endfor %}
{% endblock %}
